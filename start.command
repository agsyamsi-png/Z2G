#!/usr/bin/env bash
# ==============================================================================
# Z2G - Double-Clickable macOS App Launcher
# Simply double-click this file in Finder to run the migration platform!
# ==============================================================================

cd "$(dirname "$0")" || exit 1

# Execute run.sh
./run.sh
